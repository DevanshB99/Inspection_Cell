disc_to_ur5e:
  # kinematics_solver_timeout was the unmodified Setup Assistant default
  # (0.005s / 5ms), which is far too short for KDL's numerical IK to converge
  # on this 7-DOF redundant chain (turntable + 6 UR5e joints) for a 6-DOF
  # camera pose goal. OMPL's pose-goal sampler calls this solver repeatedly
  # while building the goal tree, so a too-short timeout shows up as
  # "Unable to sample any valid states for goal tree" for goals that are
  # often actually reachable. Raised to give IK more room to converge; this
  # group is only used for offline planning (viewpoint_traversal_node), not
  # the real-time Servo loop, so latency here is not safety-critical.
  kinematics_solver: kdl_kinematics_plugin/KDLKinematicsPlugin
  kinematics_solver_search_resolution: 0.0050000000000000001
  kinematics_solver_timeout: 0.1
turntable:
  kinematics_solver: kdl_kinematics_plugin/KDLKinematicsPlugin
  kinematics_solver_search_resolution: 0.0050000000000000001
  kinematics_solver_timeout: 0.1
ur5e:
  # DIAGNOSTIC (servo branch-jump): switched from CachedKDLKinematicsPlugin to plain KDL.
  # MoveIt Servo's jointDeltaFromIK calls searchPositionIK(seed=current_joint_positions,
  # timeout=publish_period/2=1ms, return_approximate_solution=true) and returns
  # delta_theta = solution - current. The Cached plugin overrides the provided seed with
  # the nearest cached solution, which can be a different IK branch, producing a large
  # delta on elbow/wrist_1/wrist_3 that /publish_period(0.002) saturates at +/-pi. Plain
  # KDL respects the provided seed, so a near-current target stays near current.
  kinematics_solver: kdl_kinematics_plugin/KDLKinematicsPlugin
  kinematics_solver_search_resolution: 0.0050000000000000001
  kinematics_solver_timeout: 0.0050000000000000001